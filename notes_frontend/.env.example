# Frontend configuration
# Base URL to the FastAPI backend (no trailing slash), e.g. https://your-backend.example.com
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# Public site URL (used for auth redirects if needed)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
